import * as utils from 'utils';

module.exports.observe = (element, config, callback) => {
	new MutationObserver((mutations) => {
		callback(mutations);
	}).observe(element, config);
	callback([]);
};
