"use strict";

Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.default = {
	subscribe: function subscribe(callback, target, config) {
		var observer = new MutationObserver(callback);

		observer.observe(target, config);

		return {
			unsubscribe: function unsubscribe() {
				observer.disconnect();
			}
		};
	}
};