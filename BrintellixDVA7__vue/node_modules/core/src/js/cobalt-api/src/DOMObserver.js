export default {
	subscribe: (callback, target, config) => {
		const observer = new MutationObserver(callback);
		
		observer.observe(target, config);

		return {
			unsubscribe: () => {
				observer.disconnect();
			}
		}
	}
}