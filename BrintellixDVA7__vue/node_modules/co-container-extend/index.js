var createComponent = require('component').createComponent,
	ExtendContainer = require('./src/js/extend-container.js').ExtendContainer,
	rivets = require('rivets');

module.exports = createComponent({
	name: 'co-container-extend',
	constructor: ExtendContainer,
	localizationTemplate: resolve('i18n/{lang}.json')
});