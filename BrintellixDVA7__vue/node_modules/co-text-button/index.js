var createComponent = require('component').createComponent,
	TextButton = require('./src/js/text-button').TextButton;

module.exports = createComponent({
	name: 'co-text-button',
	constructor: TextButton,
	localizationTemplate: resolve('i18n/{lang}.json'),
	localExtensions: {
		components: {
			"co-button": require("co-button"),
			"co-text": require("co-text")
		}
	}
});
